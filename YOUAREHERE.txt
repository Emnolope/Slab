You just got the node stuff to work on the server, the old code is still in ./old/index.html

The next thing to do is use THREE.js TextGeometery directly instead of using A-frames encapsulization, because already at that level you can mergebuffergeometries. 